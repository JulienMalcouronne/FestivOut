
  <div style="width: 350px; height: 600px; position: sticky; top: 0;"
      data-controller="mapbox"
      data-mapbox-markers-value="<%= @markers.to_json %>"
      data-mapbox-interests-value="<%= @interests.to_json %>"
      data-mapbox-users-value="<%= @users.to_json %>"
      data-mapbox-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
