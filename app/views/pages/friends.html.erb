<h1>Hello <%= @user.first_name %></h1>
<%= link_to "Add a friend", "/friends/new" %>
<br>
<h2>Find all your (<%= @friends.length %>) friends below :</h2>

  <div class="friends">
    <% if current_user.friends != nil %>
    <% friends = current_user.friends %>
    <% friends.each do |friend| %>
    <li><img src="<%= cl_image_path friend.user.avatar.key, height: 10 %>" alt=""> <%= friend.user["username"] %></li>
    <% end  %>
    <% end  %>
  </div>
