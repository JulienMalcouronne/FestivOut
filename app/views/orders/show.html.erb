<div class="container">
  <div class="row py-4">
    <div class="col-md-3">
    <%# <%= image_tag @order.teddy.photo_url, width: '100%' %>
    <%# testttt %>
    </div>
    <div class="col">
      <h1>Order #<%= @order.id %> <small><span class="badge badge-primary">Accepted</span></small></h1>
      <ul class="list-unstyled">
        <li><strong>ISSUED ON</strong> <%= @order.created_at.to_date %></li>
        <li><strong>ITEM</strong> <%= @order.item.name %> [<%= @order.item_sku %>]</li>
        <li><strong>TOTAL</strong> <%= humanized_money_with_symbol @order.amount %></li>
        </li>
      </ul>
      <p>Pleate meet your order at the Take Away area in <strong>15 minutes</strong></p>
    </div>
  </div>
</div>
