<div class="container">
  <div class="row py-4">
    <div class="col-md-3">
    <%# <%= image_tag @order.teddy.photo_url, width: '100%' %>
    <%# testttt %>
    </div>
    <div class="col" style="margin-top:40%;background-color:#181F37;padding:20px;margin-left:15px;margin-right:15px;border-radius:8px;display:flex;flex-direction: column;
    align-content: flex-start;">
      <h1><span style="color:#8661C1;">Order #<%= @order.id %> <small><span class="badge badge-primary">Accepted</span></small></h1>
      <ul class="list-unstyled">
        <li><strong>ISSUED ON</strong> <%= @order.created_at.to_date %></li>
        <li><strong>ITEM</strong> <%= @order.item.name %> </li>
        <li><strong>TOTAL</strong> <%= humanized_money_with_symbol @order.amount %></li>
        </li>
      </ul>
      <p>Please meet us at the take away with the ticket in <strong>15 minutes</strong></p>
    </div>
  </div>
</div>
